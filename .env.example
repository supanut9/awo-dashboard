# The same cluster `awo publish` writes to. A read-only user scoped to this one
# database is enough — the dashboard never writes.
MONGODB_URI=mongodb+srv://user:pass@cluster.example.mongodb.net
MONGODB_DB=awo
MONGODB_COLLECTION_PREFIX=awo_
